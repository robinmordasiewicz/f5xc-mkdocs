# ============================================================================
# Material for MkDocs - Optimized Requirements (2026)
# ============================================================================
# Updated: January 10, 2026
# Material Version: 9.7.0 (maintenance mode, fully-featured, free)
# Backup: requirements.txt.backup
# Conservative option: requirements-conservative.txt
#
# Key Changes:
# - ADDED: mkdocs-material (was missing!)
# - REMOVED: mkdocs-material-extensions (deprecated)
# - ADDED: Version pins for reproducible builds
# - CONSOLIDATED: Removed redundant plugins
# ============================================================================

# ----------------------------------------------------------------------------
# Core Material for MkDocs
# ----------------------------------------------------------------------------
# Material 9.7.0 includes all features (blog, search, tags, social, optimize,
# privacy, offline, meta, group, info) - no separate installation needed
mkdocs>=1.5.0,<2.0.0
mkdocs-material>=9.7.0,<10.0.0
pymdown-extensions>=10.0,<11.0

# ----------------------------------------------------------------------------
# API Documentation & Code Generation
# ----------------------------------------------------------------------------
# Excellent plugin, actively maintained, used by Google, Microsoft, NVIDIA
mkdocstrings[crystal,python]>=0.30.0,<1.0.0

# ----------------------------------------------------------------------------
# Dynamic Content & Templating
# ----------------------------------------------------------------------------
mkdocs-macros-plugin>=1.0.0,<2.0.0
mkdocs-include-markdown-plugin>=7.1.8,<8.0.0

# ----------------------------------------------------------------------------
# Git Integration & Attribution
# ----------------------------------------------------------------------------
mkdocs-git-authors-plugin>=0.9.0,<1.0.0
mkdocs-git-committers-plugin-2>=2.0.0,<3.0.0
mkdocs-git-revision-date-localized-plugin>=1.2.0,<2.0.0

# ----------------------------------------------------------------------------
# Navigation & Organization
# ----------------------------------------------------------------------------
mkdocs-awesome-pages-plugin>=2.9.0,<3.0.0
mkdocs-literate-nav>=0.6.0,<1.0.0
mkdocs-section-index>=0.3.0,<1.0.0

# ----------------------------------------------------------------------------
# Media & Visualization
# ----------------------------------------------------------------------------
mkdocs-glightbox>=0.4.0,<1.0.0
lightgallery==0.5  # Only version available on PyPI
mkdocs-drawio>=1.5.4,<2.0.0  # Versions start at 1.5.4
mkdocs-asciinema-player>=1.1.0,<2.0.0

# ----------------------------------------------------------------------------
# PDF Export
# ----------------------------------------------------------------------------
# Using mkdocs-with-pdf (most feature-rich)
# Removed: mkdocs-pdf-export-plugin, mkdocs-exporter (consolidation)
mkdocs-with-pdf>=0.9.0,<1.0.0

# ----------------------------------------------------------------------------
# Content Enhancement
# ----------------------------------------------------------------------------
mkdocs-table-reader-plugin>=2.0.0,<3.0.0
mkdocs-exclude>=1.0.0,<2.0.0

# Removed redundant plugins (Material has native support):
# - mkdocs-content-tabs (Material has native tabs via pymdown-extensions)
# - mkdocs-badges (Material has native admonitions and icons)
# - mkdocs-github-admonitions-plugin (Material has native admonitions)

# Niche plugins (evaluate if actually used):
mkdocs-add-teaser>=0.9.0,<1.0.0
mkdocs-enumerate-headings-plugin>=0.6.0,<1.0.0
mkdocs-same-dir>=0.1.3,<1.0.0
mkdocs-material-mark-as-read==0.0.1  # Only version available

# ----------------------------------------------------------------------------
# Build Optimization
# ----------------------------------------------------------------------------
mkdocs-minify-plugin>=0.8.0,<1.0.0

# ----------------------------------------------------------------------------
# Supporting Libraries
# ----------------------------------------------------------------------------
# Core dependencies
babel>=2.14.0,<3.0.0
colorama>=0.4.6,<1.0.0
jinja2>=3.1.0,<4.0.0
markdown>=3.5.0,<4.0.0
pygments>=2.17.0,<3.0.0
requests>=2.31.0,<3.0.0

# PDF generation support
pydyf>=0.10.0,<1.0.0

# Utility libraries
qrcode>=7.4.0,<8.0.0
regex>=2023.12.0,<2025.0.0
paginate>=0.5.0,<1.0.0
backrefs>=5.6,<6.0

# Development tools (optional, can remove if not used)
poetry>=1.7.0,<2.0.0

# ----------------------------------------------------------------------------
# Experimental/Unclear (removed - evaluate if needed)
# ----------------------------------------------------------------------------
# Removed: docs-chat-bot (unclear maintenance status)
# Removed: markitdown (unclear purpose in MkDocs context)
# Removed: mkdocs-material-extensions (DEPRECATED)
# Removed: mkdocs-monorepo-plugin (related to deprecated projects plugin)
# Removed: mkdocs-exporter (consolidating export plugins)
# Removed: mkdocs-pdf-export-plugin (consolidating export plugins)

# ============================================================================
# Post-Installation Steps:
# ============================================================================
# 1. Generate lockfile: pip freeze > requirements.lock
# 2. Test build: mkdocs build
# 3. Enable Material plugins in mkdocs.yml:
#    plugins:
#      - search
#      - tags
#      - blog       # if needed
#      - social     # if needed
#      - optimize   # if needed
#      - privacy    # if needed
#      - offline    # if needed
# 4. Commit changes
# ============================================================================
